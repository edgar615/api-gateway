{
  "name": "user.tel.bind.1.0.0",
  "method": "POST",
  "path": "/bindtel",
  "authentication": true,
  "appkey": true,
  "app_code_vertify": true,
  "body.arg": [
    {
      "name": "tel",
      "rules": {
        "required": true,
        "maxLength": 11,
        "minLength": 11,
        "regex": "[0-9]{11}"
      }
    },
    {
      "name": "vertifyCode",
      "rules": {
        "required": true,
        "regex": "[0-9]{6}"
      }
    },
    {
      "name": "areaCode",
      "default_value" : "0086"
    }
  ],
  "endpoints": [
    {
      "service": "user",
      "method": "POST",
      "name": "bindtel",
      "type": "http",
      "path": "/users/$user.userId/bindtel"
    }
  ],
  "request.transformer": [
    {
      "name": "bindtel",
      "header.add": [
        "x-auth-userId:$user.userId",
        "x-auth-companyCode:$user.companyCode",
        "x-policy-owner:individual"
      ]
    }
  ]
}