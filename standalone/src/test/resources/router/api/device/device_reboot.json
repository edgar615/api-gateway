{
  "name": "device.reboot.1.0.0",
  "method": "POST",
  "path": "/devices/(\\d+)/reboot",
  "authentication": true,
  "scope": "device:write",
  "appkey": true,
  "app_code_vertify": true,
  "endpoints": [
    {
      "service": "device",
      "method": "POST",
      "name": "reboot_device",
      "type": "http",
      "path": "/devices/$var.param0/reboot"
    }
  ],
  "request.transformer": [
    {
      "name": "reboot_device",
      "header.add": [
        "x-auth-userId:$user.userId",
        "x-auth-companyCode:$user.companyCode",
        "x-policy-owner:individual"
      ]
    }
  ]
}